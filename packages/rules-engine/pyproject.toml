[project]
name = "ambyte-rules"
version = "0.1.0"
description = "Logic for policy conflict resolution and enforcement"
requires-python = ">=3.11"
dependencies = [
    "ambyte-schemas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
ambyte-schemas = { workspace = true }